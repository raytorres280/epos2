import * as React from 'react';

export interface AppProps {
}

export default class NewOrder extends React.Component<any, any> {
  render() {
    return (
      <div style={{ width: '100%'}}>
        new order screen
      </div>
    );
  }
}
